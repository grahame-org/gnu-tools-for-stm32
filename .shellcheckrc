# ShellCheck configuration for gnu-tools-for-stm32 build scripts.

# Bulk suppressions have been implemented to support adding shellcheck
# to the CI without generating a lot of noise. They shall be incrementally
# fixed in future PRs
#
# shell=bash sets the default shell for all scripts in this repository.
# build-common.sh (which is sourced rather than executed directly) also carries
# an explicit "# shellcheck shell=bash" directive for clarity.
shell=bash

# Globally suppress findings that are intentional legacy patterns across all
# three build scripts and are not masking real bugs:
#
#   SC2034 – variable assigned but never used: the build-common.sh "setup"
#            section intentionally sets exported env-var constants consumed by
#            the sourcing scripts (build-toolchain.sh, build-prerequisites.sh).
#
disable=SC2034
